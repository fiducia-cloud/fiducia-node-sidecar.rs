// Generated by flags-2-env from .cli-flags.toml. Do not edit.

import 'dart:io';

final class SidecarEnvValues {
  const SidecarEnvValues({required this.allowNonLoopback, required this.bind, required this.fiduciaAz, required this.fiduciaBrainUrl, required this.fiduciaExportTarget, required this.fiduciaInternalSecret, required this.fiduciaLogSink, required this.fiduciaLogSource, required this.fiduciaNodeAddress, required this.fiduciaNodeId, required this.fiduciaNodeLogSource, required this.fiduciaNodeUrl, required this.fiduciaNodeVersion, required this.fiduciaRack, required this.fiduciaRegion, required this.fiduciaSidecarRole, required this.port});

  final bool allowNonLoopback;
  final String bind;
  final String? fiduciaAz;
  final String? fiduciaBrainUrl;
  final String? fiduciaExportTarget;
  final String? fiduciaInternalSecret;
  final String? fiduciaLogSink;
  final String? fiduciaLogSource;
  final String? fiduciaNodeAddress;
  final String? fiduciaNodeId;
  final String? fiduciaNodeLogSource;
  final String? fiduciaNodeUrl;
  final String? fiduciaNodeVersion;
  final String? fiduciaRack;
  final String? fiduciaRegion;
  final String? fiduciaSidecarRole;
  final String? port;
}

/// Pure: resolve values from an explicit lookup.
SidecarEnvValues loadFrom(String? Function(String key) lookup) {
  return SidecarEnvValues(
    allowNonLoopback: _parseBool(lookup('ORES_OTEL_SIDECAR_ALLOW_NON_LOOPBACK'), false),
    bind: _nonEmpty(lookup('FIDUCIA_SIDECAR_BIND')) ?? '127.0.0.1:9090',
    fiduciaAz: _nonEmpty(lookup('FIDUCIA_AZ')),
    fiduciaBrainUrl: _nonEmpty(lookup('FIDUCIA_BRAIN_URL')),
    fiduciaExportTarget: _nonEmpty(lookup('FIDUCIA_EXPORT_TARGET')),
    fiduciaInternalSecret: _nonEmpty(lookup('FIDUCIA_INTERNAL_SECRET')),
    fiduciaLogSink: _nonEmpty(lookup('FIDUCIA_LOG_SINK')),
    fiduciaLogSource: _nonEmpty(lookup('FIDUCIA_LOG_SOURCE')),
    fiduciaNodeAddress: _nonEmpty(lookup('FIDUCIA_NODE_ADDRESS')),
    fiduciaNodeId: _nonEmpty(lookup('FIDUCIA_NODE_ID')),
    fiduciaNodeLogSource: _nonEmpty(lookup('FIDUCIA_NODE_LOG_SOURCE')),
    fiduciaNodeUrl: _nonEmpty(lookup('FIDUCIA_NODE_URL')),
    fiduciaNodeVersion: _nonEmpty(lookup('FIDUCIA_NODE_VERSION')),
    fiduciaRack: _nonEmpty(lookup('FIDUCIA_RACK')),
    fiduciaRegion: _nonEmpty(lookup('FIDUCIA_REGION')),
    fiduciaSidecarRole: _nonEmpty(lookup('FIDUCIA_SIDECAR_ROLE')),
    port: _nonEmpty(lookup('PORT')),
  );
}

/// Resolve values from a map of env keys.
SidecarEnvValues loadFromMap(Map<String, String> env) {
  return loadFrom((key) => env[key]);
}

/// Effectful: read the process environment.
SidecarEnvValues loadFromOs() {
  return loadFromMap(Platform.environment);
}

String? _nonEmpty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

bool _parseBool(String? raw, bool fallback) {
  switch (raw?.trim()) {
    case '1':
    case 'true':
    case 'TRUE':
    case 'yes':
    case 'YES':
      return true;
    case '0':
    case 'false':
    case 'FALSE':
    case 'no':
    case 'NO':
      return false;
    default:
      return fallback;
  }
}

int _parseInt(String? raw, int fallback) {
  return int.tryParse(raw?.trim() ?? '') ?? fallback;
}

double _parseDouble(String? raw, double fallback) {
  return double.tryParse(raw?.trim() ?? '') ?? fallback;
}

String? _nonempty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

String requireEnv(String name, String expectedType, List<String> examples, String? value) {
  final trimmed = _nonempty(value);
  if (trimmed != null) {
    return trimmed;
  }
  throw MissingEnv(name: name, expectedType: expectedType, examples: examples);
}

String? pick(
  List<String> keys,
  List<String> order,
  Map<String, String> shell,
  Map<String, String> dotenv,
  Map<String, String> flags,
  String? fallback,
) {
  for (final source in order) {
    final map = source == 'flags' ? flags : source == 'env_file' ? dotenv : shell;
    for (final key in keys) {
      final value = _nonempty(map[key]);
      if (value != null) {
        return value;
      }
    }
  }
  return _nonempty(fallback);
}

String _unquote(String value) {
  final trimmed = value.trim();
  if (trimmed.length >= 2 &&
      ((trimmed.startsWith('"') && trimmed.endsWith('"')) ||
          (trimmed.startsWith("'") && trimmed.endsWith("'")))) {
    return trimmed.substring(1, trimmed.length - 1);
  }
  return trimmed;
}

Map<String, String> parseDotenv(String text) {
  final out = <String, String>{};
  for (final raw in text.split('\n')) {
    var line = raw.trim();
    if (line.isEmpty || line.startsWith('#')) {
      continue;
    }
    if (line.startsWith('export ')) {
      line = line.substring('export '.length).trim();
    }
    final eq = line.indexOf('=');
    if (eq <= 0) {
      continue;
    }
    final key = line.substring(0, eq).trim();
    if (key.isEmpty) {
      continue;
    }
    out[key] = _unquote(line.substring(eq + 1));
  }
  return out;
}

bool _dotenvEnabled() {
  final value = Platform.environment['FLAGS2ENV_DOTENV'];
  return value != '0' && value != 'false' && value != 'FALSE' && value != 'no' && value != 'NO';
}

Map<String, String> loadDotenvFiles(List<String> files) {
  if (!_dotenvEnabled()) {
    return {};
  }
  return files.fold<Map<String, String>>({}, (acc, path) {
    try {
      acc.addAll(parseDotenv(File(path).readAsStringSync()));
    } catch (_) {}
    return acc;
  });
}
final class MissingEnv implements Exception {
  const MissingEnv({required this.name, required this.expectedType, required this.examples});
  final String name;
  final String expectedType;
  final List<String> examples;
  @override
  String toString() => 'missing required environment variable $name\n  expected type: $expectedType\n  examples: ${examples.join(', ')}';
}

Map<String, String> loadEnvMap(
  Map<String, String> shell,
  Map<String, String> dotenv, [
  Map<String, String> flags = const {},
]) {
  final out = <String, String>{};
  final allowNonLoopback = pick(['ORES_OTEL_SIDECAR_ALLOW_NON_LOOPBACK'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'false');
  if (allowNonLoopback != null) out['ORES_OTEL_SIDECAR_ALLOW_NON_LOOPBACK'] = allowNonLoopback;
  final bind = pick(['FIDUCIA_SIDECAR_BIND'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '127.0.0.1:9090');
  if (bind != null) out['FIDUCIA_SIDECAR_BIND'] = bind;
  final fiduciaAz = pick(['FIDUCIA_AZ'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaAz != null) out['FIDUCIA_AZ'] = fiduciaAz;
  final fiduciaBrainUrl = pick(['FIDUCIA_BRAIN_URL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaBrainUrl != null) out['FIDUCIA_BRAIN_URL'] = fiduciaBrainUrl;
  final fiduciaExportTarget = pick(['FIDUCIA_EXPORT_TARGET'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaExportTarget != null) out['FIDUCIA_EXPORT_TARGET'] = fiduciaExportTarget;
  final fiduciaInternalSecret = pick(['FIDUCIA_INTERNAL_SECRET'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaInternalSecret != null) out['FIDUCIA_INTERNAL_SECRET'] = fiduciaInternalSecret;
  final fiduciaLogSink = pick(['FIDUCIA_LOG_SINK'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaLogSink != null) out['FIDUCIA_LOG_SINK'] = fiduciaLogSink;
  final fiduciaLogSource = pick(['FIDUCIA_LOG_SOURCE'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaLogSource != null) out['FIDUCIA_LOG_SOURCE'] = fiduciaLogSource;
  final fiduciaNodeAddress = pick(['FIDUCIA_NODE_ADDRESS'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaNodeAddress != null) out['FIDUCIA_NODE_ADDRESS'] = fiduciaNodeAddress;
  final fiduciaNodeId = pick(['FIDUCIA_NODE_ID'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaNodeId != null) out['FIDUCIA_NODE_ID'] = fiduciaNodeId;
  final fiduciaNodeLogSource = pick(['FIDUCIA_NODE_LOG_SOURCE'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaNodeLogSource != null) out['FIDUCIA_NODE_LOG_SOURCE'] = fiduciaNodeLogSource;
  final fiduciaNodeUrl = pick(['FIDUCIA_NODE_URL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaNodeUrl != null) out['FIDUCIA_NODE_URL'] = fiduciaNodeUrl;
  final fiduciaNodeVersion = pick(['FIDUCIA_NODE_VERSION'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaNodeVersion != null) out['FIDUCIA_NODE_VERSION'] = fiduciaNodeVersion;
  final fiduciaRack = pick(['FIDUCIA_RACK'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaRack != null) out['FIDUCIA_RACK'] = fiduciaRack;
  final fiduciaRegion = pick(['FIDUCIA_REGION'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaRegion != null) out['FIDUCIA_REGION'] = fiduciaRegion;
  final fiduciaSidecarRole = pick(['FIDUCIA_SIDECAR_ROLE'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaSidecarRole != null) out['FIDUCIA_SIDECAR_ROLE'] = fiduciaSidecarRole;
  final port = pick(['PORT'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (port != null) out['PORT'] = port;
  return out;
}

const List<String> _dotenvFiles = ['.env'];

Map<String, String> loadEnvMapFromOs() {
  return loadEnvMap(Platform.environment, loadDotenvFiles(_dotenvFiles));
}
